# Test URDF assets

- **`04-materials.urdf`** — a small, self-contained robot (one cylinder base + two boxes,
  no external meshes) from the [ros/urdf_tutorial](https://github.com/ros/urdf_tutorial)
  project (BSD-licensed). Used to verify `ImportUrdfAsync`.
